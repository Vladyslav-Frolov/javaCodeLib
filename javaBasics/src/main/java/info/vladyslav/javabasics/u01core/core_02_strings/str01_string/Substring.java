package info.vladyslav.javabasics.u01core.core_02_strings.str01_string;

public class Substring {
    public static void main(String[] args) {
        String state = "{\"timestamp\":\"2021-09-07T13:45:41.805+00:00\",\"status\":401,\"error\":" +
                "\"Unauthorized\",\"message\":\"Unauthorized\",\"path\":\"/api/0/cooperation/1/houses\"}";
/*   String result = state.substring(state.con)
        String regex = "(.*)(\\d+)(.*)"; // регулярное выражение

        Pattern pattern = Pattern.compile(regex); // Создание Pattern объекта

        Matcher matcher = pattern.matcher(text); // Создание matcher объекта

        if (matcher.find( )) {
            System.out.println("Найдено значение: " + matcher.group(0));*/
    }
}
