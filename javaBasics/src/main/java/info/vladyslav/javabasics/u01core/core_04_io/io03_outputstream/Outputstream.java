package info.vladyslav.javabasics.u01core.core_04_io.io03_outputstream;

import java.io.IOException;
import java.io.OutputStream;

import static java.nio.charset.StandardCharsets.UTF_8;

public class Outputstream {

    public static void main(String[] args) {
/*        FileSystem fs = FileSystem.get(context.getConfiguration());
        try (OutputStream outputStream = fs.create(file)) {
            // Create file
            log.info("Creating export configuration to index file: {}", file);

            // Write to file
            outputStream.write("# This file maps the XML_FEED index to its generator name\n".getBytes(UTF_8));
            outputStream.write("# File is only use for development debugging purposes!\n\n".getBytes(UTF_8));
            outputStream.write(payload.getBytes(UTF_8));

        } catch (IOException ex) {
            log.error("Unable to write export configuration to index file: {}", file, ex);
        }*/
    }
}
